// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,   // disables double-invoke in dev that causes duplicate API calls
}

module.exports = nextConfig